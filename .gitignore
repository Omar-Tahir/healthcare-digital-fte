# Secrets — never commit
.env
*.key
*.pem

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# MIMIC-IV data — de-identified but gitignored per ADR-013
data/mimic/
data/icd10/
data/drg/

# Benchmark results
data/
keys/
keys/public_jwks.json

# Claude Code local config (machine-specific, not project config)
.claude/settings.local.json

# MCP server config (environment-specific)
.mcp.json

# Playwright MCP session artifacts (page snapshots, console logs)
.playwright-mcp/

# Screenshots captured during automation sessions
epic-*.png
